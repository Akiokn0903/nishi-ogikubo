(()=>{const town=document.title.replace('LOCAL ATLAS — ','');const key='local-atlas-drafts:'+town;const button=document.createElement('button');button.textContent='＋ この町に書き足す';button.style.cssText='position:fixed;right:14px;bottom:14px;z-index:70;border:0;border-radius:999px;padding:13px 17px;background:#efb9c2;color:#253028;font-weight:800;box-shadow:0 8px 28px #0002;cursor:pointer';document.body.appendChild(button);const panel=document.createElement('div');panel.className='contrib-panel';panel.innerHTML=`<div class="contrib-card"><div class="eyebrow">COMMUNITY ATLAS / ${town}</div><h2>${town}に、ひとつ残す。</h2><p>店主、住民、昔住んでいた人、旅人。おすすめランキングではなく、次の人に残したいローカル知識を。</p><label>何を足す？</label><select><option>場所・店</option><option>文化・歴史</option><option>人・記憶</option><option>散歩・移動のコツ</option><option>写真・Instagram</option><option>情報の修正</option></select><label>名前</label><input class="ctitle" placeholder="店名、場所、テーマ"><label>内容</label><textarea class="ctext" placeholder="なぜ残したい？ いつ行くといい？ 地元の人しか知らないことは？"></textarea><label>出典 / 公式・Instagram URL</label><input class="csource" placeholder="任意"><button class="save">下書きを保存</button> <button class="close">閉じる</button><div class="drafts"></div><p class="source-note">現在は公開前の投稿下書きとして端末に保存されます。共同データベース接続後、公開・履歴・差し戻しを実装する前提の投稿UIです。</p></div>`;document.body.appendChild(panel);button.onclick=()=>{panel.classList.add('open');render()};panel.querySelector('.close').onclick=()=>panel.classList.remove('open');panel.onclick=e=>{if(e.target===panel)panel.classList.remove('open')};panel.querySelector('.save').onclick=()=>{const title=panel.querySelector('.ctitle').value.trim(),text=panel.querySelector('.ctext').value.trim(),source=panel.querySelector('.csource').value.trim();if(!title&&!text)return;const arr=JSON.parse(localStorage.getItem(key)||'[]');arr.unshift({title,text,source,date:new Date().toLocaleString()});localStorage.setItem(key,JSON.stringify(arr));panel.querySelector('.ctitle').value='';panel.querySelector('.ctext').value='';panel.querySelector('.csource').value='';render()};function render(){const arr=JSON.parse(localStorage.getItem(key)||'[]');panel.querySelector('.drafts').innerHTML=arr.length?'<h3>あなたの下書き</h3>'+arr.map(x=>`<div class="draft"><b>${esc(x.title||'無題')}</b><p>${esc(x.text)}</p><small>${esc(x.date)} ${x.source?' / '+esc(x.source):''}</small></div>`).join(''):''}function esc(s=''){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}})();